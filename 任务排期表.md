# 任务排期表

> 最后更新: 2026-05-28
> 当前分支: `segearthov3`
> 当前最优: Plan7-A (Vaihingen, 77.55% mIoU / 88.20% OA, soft-logit eval)

---

## 任务1: 补全 Plan3-9 所有缺失的 256² sliding-window eval 数据

**优先级**: P0（阻塞模型对比和论文数据整理）
**预计耗时**: 1-2 小时（含脚本适配 + 推理时间）
**状态**: 进行中

### 背景

项目中 Plan6-9 的实验 eval 数据不完整，原因有两类：

1. **eval 脚本缺 per_class_recall**：部分 eval 脚本（尤其是 Phase4 系列）只输出 `avg_oa`、`avg_miou`、`per_class_iou` 三项，缺少 `per_class_recall`（即 MFNet 论文定义的 per-class OA = TP/(TP+FN)）和 `tiles` 逐 tile 明细。Plan7/Plan8 的 eval 脚本是完整的参考实现。

2. **有 checkpoint 但从未跑过 eval**：部分实验训练完成但没执行 256² 滑动窗口评估（Potsdam 系列、Plan9、部分 unfreeze 实验）。

### 当前状态

####  已完整（有 per_class_recall + tiles）
| 实验 | OA | mIoU | eval 文件 |
|------|-----|------|-----------|
| Plan6 Phase1 (202720) | 87.27 | 76.57 | eval_256_vaihingen_plan6_phase1_global.json |
| Plan7-A Vaihingen (225309) | 87.75 | 77.14 | eval_256_vaihingen_plan7_a_global.json |
| Plan7-B1 slope (203622) | 87.26 | 76.49 | eval_256_vaihingen_plan7_b1_global.json |
| Plan7-C3 eval (082938) | 87.67 | 76.06 | eval_256.json |
| Plan8 CTRL (081446) | 86.33 | 74.77 | eval_256_vaihingen_plan8_ctrl.json |
| Plan8 CA-A (131457, 203711) | 86.21~86.72 | 74.52~74.58 | eval_256_vaihingen_plan8_ca_a.json |
| Plan8 AB-A (144112) | 86.22 | 74.12 | eval_256_vaihingen_plan8_ab_a.json |

####  缺 per_class_recall（eval 脚本只输出 3 key: OA, mIoU, IoU）
| 实验 | OA | mIoU | Checkpoint 路径 |
|------|-----|------|-----------------|
| Phase4 F0 (213923) | 88.23 | 77.34 | `.../plan6_phase4_f0_vaihingen_20260518_213923/best_model.pt` |
| Phase4 F1/LoRA (124902) | 88.12 | 77.29 | `.../plan6_phase4_lora_vaihingen_20260519_124902/best_model.pt` |
| Phase4 F2/Frozen (155635) | 86.63 | 75.51 | `.../plan6_phase4_frozen_vaihingen_20260519_155635/best_model.pt` |
| Phase4 F3/Prompt (191703) | 87.28 | 76.52 | `.../plan6_phase4_prompt_vaihingen_20260519_191703/best_model.pt` |
| Phase4 F0' frozen (222933) | 86.57 | 75.29 | `.../plan6_phase4_f0p_frozen_vaihingen_20260519_222933/best_model.pt` |
| Phase4 F0'+L (093831) | 87.89 | 77.24 | `.../plan6_phase4_f0p_lora_vaihingen_20260520_093831/best_model.pt` |
| Phase4 F0'+M (223449) | 87.64 | 76.89 | `.../plan6_phase4_f0p_m_vaihingen_20260520_223449/best_model.pt` |
| Phase4 F0'+L2 (192526) | 87.57 | 77.01 | `.../plan6_phase4_f0p_l2_vaihingen_20260520_192526/best_model.pt` |

####  有 best_model.pt 但完全无 eval
| 实验 | 路径 | 数据集 |
|------|------|--------|
| Phase4 F0 Potsdam | `.../plan6_phase4_f0_potsdam_20260527_092853/best_model.pt` | Potsdam |
| Plan7-A Potsdam #1 | `.../plan7_phase_a_dsm_prompt_potsdam_20260526_201000/best_model.pt` | Potsdam |
| Plan7-A Potsdam #2 | `.../plan7_phase_a_dsm_prompt_potsdam_20260526_210230/best_model.pt` | Potsdam |
| Plan9 P9-A (唯一) | `.../plan9_p9A_vaihingen_20260526_143908/best_model.pt` | Vaihingen |
| Plan8 CTRL 旧版 | `.../plan8_ctrl_vaihingen_20260515_224604/best_model.pt` | Vaihingen |
| Phase4 u24_31 | `.../plan6_phase4_u24_31_vaihingen_20260525_211145/best_model.pt` | Vaihingen |
| Phase4 u24_31_mlp | `.../plan6_phase4_u24_31_mlp_vaihingen_20260525_232410/best_model.pt` | Vaihingen |
| Phase4 u28_31 | `.../plan6_phase4_u28_31_vaihingen_20260526_005659/best_model.pt` | Vaihingen |

### 执行计划

#### Step 1: 修复 Phase4 eval_all.py 的模型加载（已完成 per_class_recall 计算逻辑修复）

`eval_all.py` 位置: `Personal-Project/RS-SAM3-p6/phase4_mfnet_ablation/eval_all.py`

当前 `load_model()` 只支持两种 model type：
- `"f0"` → `MFNetSAM3` (from `f0_mfnet_sam3/model_f0.py`)
- `"f1"` → `Phase4AdapterModel` (from `f1_fusion/model_f1.py`)

需要新增以下 model type 的加载支持：

| model_type | 类名 | 源文件 |
|------------|------|--------|
| `"f0p"` | `FrozenSAM3DFM` | `f0p_frozen_baseline/model_f0p.py` |
| `"f0p_lora"` | `FrozenSAM3DFMLoRA` | `f0p_frozen_baseline/model_f0p_lora.py` |
| `"f0p_l2"` | `FrozenSAM3DFMLoRAv2` | `f0p_frozen_baseline/model_f0p_l2.py` |
| `"f0p_m"` | `FrozenSAM3MMLoRA` | `f0p_frozen_baseline/model_f0p_m.py` |

每个类的构造函数签名：
```python
FrozenSAM3DFM(sam3_model, num_classes=5, decode_channels=64, dropout=0.1, resolution=1008)
FrozenSAM3DFMLoRA(sam3_model, ..., lora_rank=8, lora_alpha=16.0)
FrozenSAM3DFMLoRAv2(sam3_model, ..., lora_rank=8, lora_alpha=16.0)
FrozenSAM3MMLoRA(sam3_model, ...)     # 需确认完整签名
```

参考实现：Plan7 `eval.py` (`Personal-Project/RS-SAM3-p7/phase_a_dsm_prompt/eval.py`)，该脚本输出完整的 8 key（avg_oa, avg_miou, per_class_iou, per_class_recall, tiles, checkpoint, checkpoint_epoch, checkpoint_crop_best）。

#### Step 2: 重跑 Phase4 所有 Vaihingen eval（缺 per_class_recall 的 8 个）

```bash
cd Personal-Project/RS-SAM3-p6/phase4_mfnet_ablation
python eval_all.py
```

确保输出文件包含 `per_class_recall` 和 `tiles` 字段。

#### Step 3: 为 Plan7-A Potsdam 运行 eval

Plan7 eval 脚本: `Personal-Project/RS-SAM3-p7/phase_a_dsm_prompt/eval.py`

```bash
cd Personal-Project/RS-SAM3-p7/phase_a_dsm_prompt
python eval.py --checkpoint /root/autodl-tmp/runs/plan7_phase_a_dsm_prompt_potsdam_20260526_210230/best_model.pt --dataset potsdam
```

Potsdam 测试集 tile 列表（6 张）:
```python
POTSDAM_TEST = ['4_10','5_11','2_11','3_10','6_11','7_12']
```

#### Step 4: 为 Plan9 P9-A 运行 eval

Plan9 代码目录: `Personal-Project/RS-SAM3-p9/`

需先确认：
1. Plan9 的 eval 脚本位置和模型加载方式
2. Plan9 P9-A 训练时使用的 model class 和参数
3. 是否可复用 Plan7 eval 脚本（需检查模型 forward 接口兼容性）

```bash
# 预期命令（待确认）
cd Personal-Project/RS-SAM3-p9/phase_a_lora_prompt
python eval.py --checkpoint /root/autodl-tmp/runs/plan9_p9A_vaihingen_20260526_143908/best_model.pt --dataset vaihingen
```

#### Step 5: 为 Phase4 F0 Potsdam 运行 eval

```bash
# 使用修复后的 eval_all.py（需先添加 Potsdam 数据集支持）
cd Personal-Project/RS-SAM3-p6/phase4_mfnet_ablation
# 当前 eval_all.py 只支持 Vaihingen，需扩展 dataset 参数
python eval_all.py --dataset potsdam  # 选项1：加命令行参数
# 或直接在 evaluate() 中硬编码 potsdam 路径
```

#### Step 6: 为 Phase4 unfreeze 实验（u24_31, u24_31_mlp, u28_31）运行 eval

这些实验的 model type 需确认。可能复用 `"f1"` (Phase4AdapterModel) 或需要新的 model class。

### 评估协议（MANDATORY）

所有 eval 必须遵循以下协议：
- **256² sliding window, stride=128**
- **Soft-logit accumulation**: 累计 logits 后再 argmax（NOT per-patch argmax，后者损失 ~0.4-0.8pp）
- Edge trim: `min(16, ph//4)` 像素
- `per_class_recall = TP/(TP+FN)` — 与 MFNet 论文公式一致
- 多 tile 结果使用全局累计（accumulated inter/union across all tiles），非 tile-wise 平均
- 参考实现: `Personal-Project/RS-SAM3-p7/phase_a_dsm_prompt/eval.py`

### 关键文件

| 文件 | 用途 |
|------|------|
| `Personal-Project/RS-SAM3-p6/phase4_mfnet_ablation/eval_all.py` | Phase4 统一 eval（已修复 per_class_recall，需扩展 model type） |
| `Personal-Project/RS-SAM3-p7/phase_a_dsm_prompt/eval.py` | Plan7 eval（完整参考实现） |
| `Personal-Project/RS-SAM3-p9/phase_a_lora_prompt/` | Plan9 代码 |
| `model_registry.py` | 所有 checkpoint 注册表（eval 完成后需更新） |
| `CLAUDE.md` | 项目规范 + 实验谱系（eval 完成后需更新 Current best 表） |

### 验收标准

- [ ] 所有缺失的 eval 全部补齐，输出 JSON 包含 `avg_oa`, `avg_miou`, `per_class_iou`, `per_class_recall`, `tiles`
- [ ] `model_registry.py` 中对应条目更新 eval 指标
- [ ] `CLAUDE.md` Current best 表更新（如有新记录）
- [ ] 无 eval 文件的残留目录按归档规则处理（tar.gz → `/root/autodl-tmp/archives/`）

---

## 任务2: 训练收敛优化 — 解决 continuation 训练提前停滞问题

**优先级**: P1（当前所有 continuation 实验的通用基础设施问题）
**预计耗时**: 1 天（含验证实验）
**状态**: 未开始

### 背景

经过 2026-05-28 对全部实验训练曲线的分析，发现停滞问题的根本原因不是 LR 调度，而是 **`init_from` 导致的 optimizer state 丢失**。

**全量训练曲线对比**：

```
Experiment           init      ep  best@   start     best    趋势
──────────────────────────────────────────────────────────────────
Plan6 Phase1       from scratch  20  10/20   74.8%   76.9%   ↗ 持续上升
Phase3 A3          from scratch  20   8/20   73.1%   76.2%   ⬆ 持续上升
Phase4 F1 LoRA     from scratch  12   7/12   74.7%   76.8%   ⬆ 持续上升
Phase4 F0p frozen  from scratch  12  12/12   71.7%   74.5%   ⬆ 12 epoch 全程上升
Phase4 F0          from scratch  12   3/12   75.1%   76.7%   ⬇ 偏早但仍有提升
──────────────────────────────────────────────────────────────────
→ F0'+L            continuation   8   1/8    76.8%   76.8%   ⬇ epoch 1 即峰值
→ Plan7-A          continuation  15   3/15   76.7%   76.9%   ➖ epoch 3 即峰值
→ Plan7-C3         continuation   8   2/8    77.1%   77.1%   ➖ 完全持平
→ Plan9 P9-A       continuation   8   1/8    76.4%   76.4%   ➖ epoch 1 即峰值
```

**结论**：
- 所有 `from scratch` 实验（上 5 行）：持续改善，best epoch 通常在中后期
- 所有 `continuation` 实验（下 4 行）：epoch 1-3 即触顶，之后再无法超越
- **同一个架构**（F0p frozen → F0'+L），from scratch 版本 12 epoch 全程上升，continuation 版本 epoch 1 即峰值——差异只在于 `init_from`

**根因**：

当前所有 continuation 训练只加载 model state_dict，**不加载 optimizer state**。AdamW 的二阶矩 `v_t` 从零开始 → 头几个 step 的有效学习率异常大（`α/(√0 + ε)` → `α/ε`） → 模型被推离已训练好的起始点 → Cosine 从 epoch 1 就开始衰减 lr → 推远了回不来 → epoch 1 就是峰值。

```
加载 ckpt 后的前几步:
  step 1: v_t ≈ 0,    有效 lr ≈ α/ε        → 大跳跃
  step 2: v_t ≈ g²,   有效 lr ≈ α/|g|      → 还算大
  step 3-10: v_t 逐渐累积, 有效 lr 逐渐下降   → 但方向已经偏了
  step 100+: v_t 稳定, lr 已被 Cosine 衰减   → 回不去了
```

对比原始 MFNet 的 SGD：SGD 没有二阶矩，lr 单纯由 MultiStepLR 控制。从 ckpt 续训时 lr 仍在 0.01，步长一致，不会有"头几步异常大"的问题。

### 执行计划

#### Step 1: 确认父 checkpoint 是否保存了 optimizer state

```bash
python3 -c "
import torch
ckpt = torch.load('/root/autodl-tmp/runs/plan6_phase4_f0p_frozen_vaihingen_20260519_222933/best_model.pt', 
                   map_location='cpu', weights_only=False)
print('Keys:', list(ckpt.keys()))
print('Has optimizer:', 'optimizer' in ckpt)
print('Has scheduler:', 'scheduler' in ckpt)
"
```

如果没有 optimizer key，需要先补保存（用 F0p frozen 训练脚本重跑一个 epoch 或手动保存 optimizer state）。

#### Step 2: 修改 continuation 训练脚本的 ckpt 加载

改动位置（3 个文件，同一处逻辑）：

```python
# 改前
ckpt = torch.load(CKPT, map_location="cpu", weights_only=False)
model.load_state_dict(ckpt['model'], strict=False)

# 改后
ckpt = torch.load(CKPT, map_location="cpu", weights_only=False)
model.load_state_dict(ckpt['model'], strict=False)
if ckpt.get('optimizer'):
    opt.load_state_dict(ckpt['optimizer'])
if ckpt.get('scheduler'):
    sch.load_state_dict(ckpt['scheduler'])
if ckpt.get('scaler'):
    scaler.load_state_dict(ckpt['scaler'])
print(f"Loaded optimizer/scheduler/scaler state from checkpoint")
```

#### Step 3: 验证实验（1 次）

用修复后的 F0'+L 代码从 F0p frozen 续训 8 epoch：

| 实验 | 预期 |
|------|------|
| F0'+L + 加载 optimizer state | best epoch 出现在 epoch 3+，后半段 mIoU 均值 ≥ 前半段 |

对比标准：当前 F0'+L（不加载 opt state）epoch 1 即峰值 76.81%。修复后应该看到类似 F0p frozen 的持续上升趋势，且最终 mIoU ≥ 77.24%（原基线）。

#### Step 4: 推广到所有 continuation 脚本

| 文件 | 说明 |
|------|------|
| `.../f0p_frozen_baseline/train_f0p_lora.py` | F0'+L（Step 3 已验证） |
| `.../phase_a_dsm_prompt/train_a.py` | Plan7-A continuation |
| `.../phase_a_lora_prompt/train_p9.py` | Plan9 continuation |
| `.../f0p_frozen_baseline/train_f0p.py` | F0p frozen — 确保保存 opt state |

#### Step 5: 补充——确保所有 from scratch 训练的 ckpt 保存 optimizer state

```python
# 所有 train 脚本的 save 逻辑改为
torch.save({
    'model': model.state_dict(),
    'optimizer': opt.state_dict(),
    'scheduler': sch.state_dict() if sch else None,
    'scaler': scaler.state_dict() if scaler else None,
    'epoch': epoch,
}, path)
```

### 验收标准

- [ ] Step 3 验证通过：continuation 加载 optimizer state 后 best epoch ≥ 3
- [ ] 修复后 F0'+L mIoU ≥ 原 baseline 77.24%
- [ ] 4 个训练脚本全部更新
- [ ] CLAUDE.md 记录此规范：continuation 训练必须加载 optimizer state
